# Core template

An example project with a backend, frontend, and worker.

## Project Structure

- `backend/`: Example Express API server
- `frontend/`: Simple Next.js frontend
- `worker/`: Skeleton worker
- `shared/`: Shared logic
